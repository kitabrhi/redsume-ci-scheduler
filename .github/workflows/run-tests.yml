name: ReDsume — Tests BDD (V1 Cypress · V2 Playwright)

# ─────────────────────────────────────────────────────────────────────────────
# Ce repo public sert uniquement de scheduler CI.
# Il checkout le repo privé (via PAT) et lance les tests.
# Le code source privé n'est jamais exposé publiquement.
# ─────────────────────────────────────────────────────────────────────────────

on:
  workflow_dispatch:

  schedule:
    - cron: '0 16 * * 1'   # Lundi 16h00
    - cron: '0 11 * * 2'   # Mardi 11h00
    - cron: '0 16 * * 2'   # Mardi 16h00
    - cron: '0  7 * * 3'   # Mercredi 07h00
    - cron: '0 11 * * 3'   # Mercredi 11h00
    - cron: '0 15 * * 3'   # Mercredi 15h00
    - cron: '0 17 * * 3'   # Mercredi 17h00
    - cron: '0  7 * * 4'   # Jeudi 07h00
    - cron: '0 11 * * 4'   # Jeudi 11h00
    - cron: '0 13 * * 4'   # Jeudi 13h00
    - cron: '0 15 * * 4'   # Jeudi 15h00
    - cron: '0  7 * * 5'   # Vendredi 07h00
    - cron: '0 10 * * 5'   # Vendredi 10h00
    - cron: '0 13 * * 5'   # Vendredi 13h00
    - cron: '0 15 * * 5'   # Vendredi 15h00
    - cron: '0 17 * * 5'   # Vendredi 17h00
    - cron: '0  8 * * 6'   # Samedi 08h00
    - cron: '0 14 * * 6'   # Samedi 14h00

jobs:

  # ───────────────────────────────────────────────────────────────────────────
  # JOB 1 : V1 — Cypress
  # ───────────────────────────────────────────────────────────────────────────
  cypress-v1:
    name: "V1 · Cypress (not @v2-only)"
    runs-on: ubuntu-latest

    steps:
      - name: Checkout du repo privé
        uses: actions/checkout@v4
        with:
          repository: ${{ secrets.PRIVATE_REPO }}
          token: ${{ secrets.PRIVATE_REPO_TOKEN }}

      - name: Installation de Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Installation des dépendances
        run: npm ci

      - name: Lancer les tests V1 (Cypress)
        id: tests_v1
        uses: cypress-io/github-action@v6
        with:
          browser: chrome
          headed: false
        env:
          CYPRESS_BASE_URL:            ${{ secrets.CYPRESS_BASE_URL }}
          CYPRESS_APP_VERSION:         v1
          CYPRESS_TEST_USER_EMAIL:     ${{ secrets.TEST_USER_EMAIL }}
          CYPRESS_TEST_USER_FULLNAME:  ${{ secrets.TEST_USER_FULLNAME }}
          CYPRESS_USER_LOGIN:          ${{ secrets.CYPRESS_USER_LOGIN }}
          CYPRESS_USER_PASSWORD:       ${{ secrets.CYPRESS_USER_PASSWORD }}

      - name: Upload rapports V1
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: cypress-v1-${{ github.run_number }}
          path: |
            cypress/reports/
            cypress/screenshots/
          retention-days: 7

      # ── Log commit → repo PUBLIC (scheduler) avec SCHEDULER_PAT ──────────
      - name: Checkout du repo scheduler (public)
        if: always()
        uses: actions/checkout@v4
        with:
          repository: kitabrhi/redsume-ci-scheduler
          token: ${{ secrets.SCHEDULER_PAT }}
          path: scheduler

      - name: Commit du log dans le repo scheduler
        if: always()
        run: |
          cd scheduler
          STATUS="${{ steps.tests_v1.outcome }}"
          ICON=$( [ "$STATUS" = "success" ] && echo "pass" || echo "FAIL" )
          echo "Run #${{ github.run_number }} — V1 Cypress [$ICON] — $(date -u)" >> run-history.log
          git config user.name  "Youssef Kitabrhi"
          git config user.email "kitabrhi.youssef.1@gmail.com"
          git add run-history.log
          git commit -m "ci: run #${{ github.run_number }} — V1 Cypress [$ICON]" || echo "Nothing to commit"
          for i in 1 2 3 4 5; do
            git pull --rebase origin main && git push origin main && break
            sleep 5
          done

  # ───────────────────────────────────────────────────────────────────────────
  # JOB 2 : V2 — Playwright
  # ───────────────────────────────────────────────────────────────────────────
  playwright-v2:
    name: "V2 · Playwright (not @v1-only)"
    runs-on: ubuntu-latest

    steps:
      - name: Checkout du repo privé
        uses: actions/checkout@v4
        with:
          repository: ${{ secrets.PRIVATE_REPO }}
          token: ${{ secrets.PRIVATE_REPO_TOKEN }}

      - name: Installation de Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Installation des dépendances
        run: npm ci

      - name: Installation de Chromium (Playwright)
        run: npx playwright install chromium --with-deps

      - name: Lancer les tests V2 (Playwright)
        id: tests_v2
        run: npx cucumber-js --config playwright/cucumber.cjs
        env:
          BASE_URL:           ${{ secrets.PLAYWRIGHT_BASE_URL }}
          TEST_USER_EMAIL:    ${{ secrets.TEST_USER_EMAIL }}
          TEST_USER_FULLNAME: ${{ secrets.TEST_USER_FULLNAME }}
          USER_LOGIN:         ${{ secrets.CYPRESS_USER_LOGIN }}
          USER_PASSWORD:      ${{ secrets.CYPRESS_USER_PASSWORD }}

      - name: Résumé des résultats V2
        if: always()
        run: |
          node - <<'EOF'
          const fs = require('fs');
          const path = 'playwright/reports/results.json';
          if (!fs.existsSync(path)) {
            fs.appendFileSync(process.env.GITHUB_STEP_SUMMARY,
              '## V2 Playwright\n\n> ⚠️ Aucun fichier de résultats.\n');
            process.exit(0);
          }
          const results = JSON.parse(fs.readFileSync(path, 'utf8'));
          let passed = 0, failed = 0, pending = 0;
          let totalNs = 0;
          for (const f of results) {
            for (const s of (f.elements || [])) {
              if (s.type === 'background') continue;
              const statuses = (s.steps || []).map(x => x.result?.status || 'skipped');
              if (statuses.some(x => x === 'failed')) failed++;
              else if (statuses.some(x => x === 'pending' || x === 'undefined')) pending++;
              else passed++;
              for (const st of (s.steps || [])) totalNs += st.result?.duration || 0;
            }
          }
          const dur = (totalNs / 1e9).toFixed(1) + 's';
          const ok  = failed === 0 ? '✅ Passing' : '❌ Failing';
          const tbl = [
            '## V2 Playwright Results',
            '',
            '| Status | Passed | Failed | Pending | Duration |',
            '|--------|--------|--------|---------|----------|',
            `| ${ok} | ${passed} | ${failed} | ${pending} | ${dur} |`,
          ].join('\n');
          fs.appendFileSync(process.env.GITHUB_STEP_SUMMARY, tbl);
          console.log(tbl);
          EOF

      - name: Upload rapports V2
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-v2-${{ github.run_number }}
          path: playwright/reports/
          retention-days: 7

      # ── Log commit → repo PUBLIC (scheduler) avec SCHEDULER_PAT ──────────
      - name: Checkout du repo scheduler (public)
        if: always()
        uses: actions/checkout@v4
        with:
          repository: kitabrhi/redsume-ci-scheduler
          token: ${{ secrets.SCHEDULER_PAT }}
          path: scheduler

      - name: Commit du log dans le repo scheduler
        if: always()
        run: |
          cd scheduler
          STATUS="${{ steps.tests_v2.outcome }}"
          ICON=$( [ "$STATUS" = "success" ] && echo "pass" || echo "FAIL" )
          echo "Run #${{ github.run_number }} — V2 Playwright [$ICON] — $(date -u)" >> run-history.log
          git config user.name  "Youssef Kitabrhi"
          git config user.email "kitabrhi.youssef.1@gmail.com"
          git add run-history.log
          git commit -m "ci: run #${{ github.run_number }} — V2 Playwright [$ICON]" || echo "Nothing to commit"
          for i in 1 2 3 4 5; do
            git pull --rebase origin main && git push origin main && break
            sleep 5
          done
